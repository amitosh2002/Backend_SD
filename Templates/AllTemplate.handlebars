<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hora Email Templates</title>
    <style>
        /* Base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #f8fafc;
            margin: 0;
            padding: 20px;
        }
        
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 40px 30px;
            text-align: center;
            color: white;
        }
        
        .logo {
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }
        
        .tagline {
            font-size: 14px;
            opacity: 0.9;
            font-weight: 300;
        }
        
        .content {
            padding: 40px 30px;
        }
        
        .template-selector {
            margin-bottom: 30px;
            text-align: center;
        }
        
        .template-buttons {
            display: flex;
            gap: 10px;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 20px;
        }
        
        .template-btn {
            padding: 10px 20px;
            border: 2px solid #667eea;
            background: white;
            color: #667eea;
            border-radius: 25px;
            cursor: pointer;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .template-btn.active {
            background: #667eea;
            color: white;
        }
        
        .template-btn:hover {
            background: #667eea;
            color: white;
        }
        
        .template {
            display: none;
        }
        
        .template.active {
            display: block;
        }
        
        h1 {
            color: #2d3748;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 20px;
            line-height: 1.3;
        }
        
        h2 {
            color: #4a5568;
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 16px;
            line-height: 1.4;
        }
        
        p {
            color: #718096;
            line-height: 1.6;
            margin-bottom: 16px;
            font-size: 16px;
        }
        
        .otp-box {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-align: center;
            padding: 30px;
            border-radius: 12px;
            margin: 30px 0;
        }
        
        .otp-code {
            font-size: 42px;
            font-weight: 700;
            letter-spacing: 8px;
            margin: 10px 0;
            font-family: 'Courier New', monospace;
        }
        
        .otp-expiry {
            font-size: 14px;
            opacity: 0.9;
            margin-top: 10px;
        }
        
        .button {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            padding: 16px 32px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            margin: 20px 0;
            transition: transform 0.2s ease;
        }
        
        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }
        
        .status-badge {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            margin: 10px 0;
        }
        
        .status-pending {
            background-color: #fed7aa;
            color: #9a3412;
        }
        
        .status-progress {
            background-color: #bae6fd;
            color: #0c4a6e;
        }
        
        .status-completed {
            background-color: #bbf7d0;
            color: #14532d;
        }
        
        .status-rejected {
            background-color: #fecaca;
            color: #991b1b;
        }
        
        .ticket-details {
            background-color: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        
        .ticket-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #e2e8f0;
        }
        
        .ticket-row:last-child {
            border-bottom: none;
        }
        
        .ticket-label {
            font-weight: 600;
            color: #4a5568;
        }
        
        .ticket-value {
            color: #718096;
            font-family: 'Courier New', monospace;
        }
        
        .security-note {
            background-color: #fef5e7;
            border-left: 4px solid #f6ad55;
            padding: 16px;
            margin: 20px 0;
            border-radius: 0 8px 8px 0;
        }
        
        .security-note p {
            color: #975a16;
            margin: 0;
            font-size: 14px;
        }
        
        .footer {
            background-color: #2d3748;
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .footer-links {
            margin: 20px 0;
        }
        
        .footer-links a {
            color: #a0aec0;
            text-decoration: none;
            margin: 0 15px;
            font-size: 14px;
        }
        
        .footer-links a:hover {
            color: white;
        }
        
        .social-icons {
            margin-top: 20px;
        }
        
        .social-icon {
            display: inline-block;
            width: 40px;
            height: 40px;
            background-color: #4a5568;
            border-radius: 50%;
            margin: 0 5px;
            text-align: center;
            line-height: 40px;
            color: white;
            text-decoration: none;
        }
        
        .social-icon:hover {
            background-color: #667eea;
        }
        
        /* Responsive */
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            
            .header, .content, .footer {
                padding: 20px;
            }
            
            .otp-code {
                font-size: 32px;
                letter-spacing: 4px;
            }
            
            .template-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .template-btn {
                width: 200px;
                margin-bottom: 10px;
            }
            
            .ticket-row {
                flex-direction: column;
                align-items: flex-start;
                gap: 4px;
            }
        }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header -->
        <div class="header">
            <div class="logo">Hora</div>
            <div class="tagline">Time Tracking Made Simple</div>
        </div>
        
        <div class="content">
            <!-- Template Selector -->
            <div class="template-selector">
                <h2>Email Template Preview</h2>
                <div class="template-buttons">
                    <button class="template-btn active" onclick="showTemplate('otp')">OTP Login</button>
                    <button class="template-btn" onclick="showTemplate('reset')">Password Reset</button>
                    <button class="template-btn" onclick="showTemplate('ticket')">Ticket Status</button>
                </div>
            </div>
            
            <!-- OTP Login Template -->
            <div id="otp" class="template active">
                <h1>🔐 Your Login Code</h1>
                <p>Hi <strong>{{user_name}}</strong>,</p>
                <p>You've requested to log in to your Hora account. Use the verification code below to complete your login:</p>
                
                <div class="otp-box">
                    <div>Your verification code is:</div>
                    <div class="otp-code">{{otp_code}}</div>
                    <div class="otp-expiry">This code expires in 10 minutes</div>
                </div>
                
                <div class="security-note">
                    <p><strong>🛡️ Security Note:</strong> Never share this code with anyone. Hora support will never ask for your verification code.</p>
                </div>
                
                <p>If you didn't request this code, please ignore this email or contact our support team if you have concerns about your account security.</p>
                
                <p>Happy time tracking!<br>
                <strong>The Hora Team</strong></p>
            </div>
            
            <!-- Password Reset Template -->
            <div id="reset" class="template">
                <h1>🔑 Reset Your Password</h1>
                <p>Hi <strong>{{user_name}}</strong>,</p>
                <p>We received a request to reset the password for your Hora account. Click the button below to create a new password:</p>
                
                <div style="text-align: center;">
                    <a href="{{reset_link}}" class="button">Reset Password</a>
                </div>
                
                <p>This password reset link will expire in 1 hour for security reasons.</p>
                
                <div class="security-note">
                    <p><strong>🛡️ Security Tips:</strong></p>
                    <p>• Use a strong password with at least 8 characters</p>
                    <p>• Include uppercase, lowercase, numbers, and symbols</p>
                    <p>• Don't reuse passwords from other accounts</p>
                </div>
                
                <p>If you didn't request a password reset, you can safely ignore this email. Your password will remain unchanged.</p>
                
                <p>Need help? Contact our support team at support@hora.com</p>
                
                <p>Best regards,<br>
                <strong>The Hora Team</strong></p>
            </div>
            
            <!-- Ticket Status Template -->
            <div id="ticket" class="template">
                <h1>🎫 Ticket Status Update</h1>
                <p>Hi <strong>{{user_name}}</strong>,</p>
                <p>Your support ticket has been updated. Here are the latest details:</p>
                
                <div class="ticket-details">
                    <div class="ticket-row">
                        <span class="ticket-label">Ticket ID:</span>
                        <span class="ticket-value">{{ticket_id}}</span>
                    </div>
                    <div class="ticket-row">
                        <span class="ticket-label">Subject:</span>
                        <span class="ticket-value">{{ticket_subject}}</span>
                    </div>
                    <div class="ticket-row">
                        <span class="ticket-label">Status:</span>
                        <span class="status-badge status-{{ticket_status_class}}">{{ticket_status}}</span>
                    </div>
                    <div class="ticket-row">
                        <span class="ticket-label">Priority:</span>
                        <span class="ticket-value">{{ticket_priority}}</span>
                    </div>
                    <div class="ticket-row">
                        <span class="ticket-label">Last Updated:</span>
                        <span class="ticket-value">{{last_updated}}</span>
                    </div>
                    <div class="ticket-row">
                        <span class="ticket-label">Assigned To:</span>
                        <span class="ticket-value">{{assigned_to}}</span>
                    </div>
                </div>
                
                <div style="background-color: #f8fafc; padding: 20px; border-radius: 8px; margin: 20px 0;">
                    <h3 style="color: #2d3748; margin-bottom: 10px;">📝 Latest Update:</h3>
                    <p style="margin: 0; font-style: italic; color: #4a5568;">"{{latest_comment}}"</p>
                    <p style="margin: 10px 0 0 0; font-size: 14px; color: #718096;">- {{commenter_name}} at {{comment_time}}</p>
                </div>
                
                <div style="text-align: center;">
                    <a href="{{ticket_url}}" class="button">View Ticket Details</a>
                </div>
                
                <p>You can also reply to this email to add a comment to your ticket.</p>
                
                <p>Thank you for using Hora!</p>
                
                <p>Best regards,<br>
                <strong>The Hora Support Team</strong></p>
            </div>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <div class="logo" style="font-size: 20px; margin-bottom: 10px;">Hora</div>
            <p style="color: #a0aec0; font-size: 14px; margin-bottom: 0;">
                Making time tracking simple and efficient for teams worldwide.
            </p>
            
            <div class="footer-links">
                <a href="{{app_url}}">Dashboard</a>
                <a href="{{help_url}}">Help Center</a>
                <a href="{{contact_url}}">Contact Us</a>
                <a href="{{privacy_url}}">Privacy Policy</a>
            </div>
            
            <div class="social-icons">
                <a href="{{twitter_url}}" class="social-icon">𝕏</a>
                <a href="{{linkedin_url}}" class="social-icon">in</a>
                <a href="{{github_url}}" class="social-icon">🐙</a>
            </div>
            
            <p style="color: #718096; font-size: 12px; margin-top: 20px;">
                © 2024 Hora. All rights reserved.<br>
                {{company_address}}
            </p>
            
            <p style="color: #718096; font-size: 12px;">
                You're receiving this email because you have an account with Hora.<br>
                <a href="{{unsubscribe_url}}" style="color: #a0aec0;">Unsubscribe</a> from these notifications.
            </p>
        </div>
    </div>

    <script>
        function showTemplate(templateId) {
            // Hide all templates
            document.querySelectorAll('.template').forEach(template => {
                template.classList.remove('active');
            });
            
            // Remove active class from all buttons
            document.querySelectorAll('.template-btn').forEach(btn => {
                btn.classList.remove('active');
            });
            
            // Show selected template
            document.getElementById(templateId).classList.add('active');
            
            // Add active class to clicked button
            event.target.classList.add('active');
        }
    </script>
</body>
</html>